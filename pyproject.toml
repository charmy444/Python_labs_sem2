[project]
name = "lab1-task-platform"
version = "0.1.0"
description = "Laboratory work 2: task model with descriptors and properties"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.4.2",
]
