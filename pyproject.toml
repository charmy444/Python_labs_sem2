[project]
name = "lab3-task-platform"
version = "0.1.0"
description = "Laboratory work 3: task queue with iterators and generators"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
]
